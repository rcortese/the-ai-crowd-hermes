# Richmond Hermes tools

Installed in the image:

- git
- jq / ripgrep / sqlite3
- Python 3 + venv
- poppler-utils for PDF text/image extraction support

Intentionally absent from the production runtime:

- Docker socket
- host SSH keys
- messaging send tools
- broad runtime control tools
