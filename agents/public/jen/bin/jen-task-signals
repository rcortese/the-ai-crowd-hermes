#!/usr/bin/env bash
set -euo pipefail

workspace_root=$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)
runtime="$workspace_root/bin/jen-task-runtime"

exec "$runtime" classify-interaction-signals "$@"
