#!/usr/bin/env bash
set -euo pipefail

# Legacy text fixture used by jen-todoist-overdue-semantics.contract.sh.
# This file is intentionally not named *.contract.sh because it is not a
# standalone executable suite in the public scaffold.

# Due/overdue language must avoid guilt by default while still preserving accountability.

echo "jen-morning-copy-contract: compatibility fixture only"
