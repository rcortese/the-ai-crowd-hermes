# Roy tool posture

Use the smallest tool surface needed to classify and route intake. External channel writes, live credential binding, queue ACK/cursor mutation, and legacy state import require explicit operator authorization.
