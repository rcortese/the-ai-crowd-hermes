# Project .env template. Copy to .env with mode 0600 before container creation.
# Values below are placeholders only; do not commit or paste real values into Kanban.
HONCHO_POSTGRES_PASSWORD=<generate-private-value>
LITELLM_MASTER_KEY=<generate-private-value>
HONCHO_EMBEDDINGS_API_KEY=<operator-approved-embedding-key>
HONCHO_AUTH_USE_AUTH=false
