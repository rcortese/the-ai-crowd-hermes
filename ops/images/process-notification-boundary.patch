diff --git a/gateway/run.py b/gateway/run.py
index a5fe58f97..c3cf20898 100644
--- a/gateway/run.py
+++ b/gateway/run.py
@@ -10835,0 +10836,18 @@ class GatewayRunner(GatewayAuthorizationMixin, GatewayKanbanWatchersMixin, Gatew
+        # Process-watcher and async-delegation notifications are generated by
+        # Hermes, not authored by the session user.  Preserve their content as
+        # ephemeral system context for this turn, while keeping the user turn
+        # empty so command/output can neither be persisted nor replayed as
+        # ``role=user``.  Do not infer this from ``internal`` alone: that flag
+        # also handles authorization bypass for other synthetic event classes.
+        if (
+            event.internal
+            and getattr(event, "metadata", {}).get("internal_process_notification")
+        ):
+            context_prompt = (
+                f"{context_prompt}\n\n"
+                f"[System note: Background process notification]\n{message_text}"
+            ).strip()
+            message_text = ""
+            persist_user_message = None
+            persist_user_timestamp = None
+
@@ -14540,0 +14559 @@ class GatewayRunner(GatewayAuthorizationMixin, GatewayKanbanWatchersMixin, Gatew
+                metadata={"internal_process_notification": True},
